## Agent skills

### Issue tracker

Issues live in GitHub Issues for `arbyazra123/motion-platform` (uses the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context — `CONTEXT-MAP.md` at the root pointing to per-package `CONTEXT.md` files (`host/`, `games/hand-sword/`, `games/pong/`, `games/tennis/`, `server/`). See `docs/agents/domain.md`.
