{
  "name": "motion-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "host",
    "games/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:host": "cd host && npm run dev",
    "dev:hand-sword": "cd games/hand-sword && npm run dev",
    "dev:tennis": "cd games/tennis && npm run dev",
    "dev:pong": "cd games/pong && npm run dev",
    "build": "npm-run-all build:*",
    "build:host": "cd host && npm run build",
    "build:games": "cd games/hand-sword && npm run build && cd ../tennis && npm run build && cd ../pong && npm run build",
    "install:all": "npm install && cd host && npm install && cd ../games/hand-sword && npm install && cd ../tennis && npm install && cd ../pong && npm install"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
